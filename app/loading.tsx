export default function Loading(){
    return (
        <main className="text-center">
            <h2 className="text-primary">Loading...</h2>
            <p>Sorry for the delay. content loading...</p>
        </main>
    );
}